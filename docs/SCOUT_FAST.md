# Scout fast vs quality

OHD Scout is Dial A (`lb pcc`). Sniper is TNSSRC E.

| profile | PCC_OPEN_RATIO | effect |
|---------|----------------|--------|
| quality | 0.31 | mixers still run when MATCH/BWT left file fat |
| fast | 0.99 | skip mixer tail; Fast seat only |

Never claim Fast for Sniper.
See SlidPhiLabs `lab/ohd/SPEED.md`.
